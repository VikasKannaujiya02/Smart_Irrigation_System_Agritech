# Configuration

Shared configuration files live here. Module-specific defaults may live inside module folders, but production deployments should load environment-specific values through the configuration manager.

