# Firmware

Arduino UNO firmware modules live here. Firmware must use the fixed LoRa SX1278 hardware, support ACK, retry, heartbeat, CRC, sequence numbers, packet verification, timeout handling, and duplicate packet detection.

